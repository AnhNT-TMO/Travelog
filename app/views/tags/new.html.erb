<% content_for :title, "#{t(".title")} · #{t("app.name")}" %>
<% content_for :topbar do %>
  <%= render "shared/topbar", title: t(".title"), crumb: t("tags.index.title") %>
<% end %>

<%= render "tags/form", tag: @tag %>
