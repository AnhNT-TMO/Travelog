{
  "name": "travelog-thumbnailer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "S3 ObjectCreated -> webp derivatives",
  "main": "src/index.mjs",
  "engines": { "node": ">=20" },
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "scripts": {
    "build": "npm ci --omit=dev --os=linux --cpu=arm64 --libc=glibc",
    "test": "node --test"
  }
}
